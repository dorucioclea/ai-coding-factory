// Integration Test Template for {{ControllerName}}
// Story: {{StoryId}}
// Generated by Claude Code

using System.Net;
using System.Net.Http.Json;
using FluentAssertions;
using Microsoft.AspNetCore.Mvc.Testing;
using Microsoft.Extensions.DependencyInjection;
using Xunit;

namespace {{Namespace}}.IntegrationTests;

public class {{ControllerName}}Tests : IClassFixture<WebApplicationFactory<Program>>
{
    private readonly HttpClient _client;
    private readonly WebApplicationFactory<Program> _factory;

    public {{ControllerName}}Tests(WebApplicationFactory<Program> factory)
    {
        _factory = factory.WithWebHostBuilder(builder =>
        {
            builder.ConfigureServices(services =>
            {
                // Replace services with test doubles if needed
                // services.RemoveAll<IExternalService>();
                // services.AddScoped<IExternalService, FakeExternalService>();
            });
        });
        _client = _factory.CreateClient();
    }

    #region GET Endpoints

    [Fact]
    [Trait("Story", "{{StoryId}}")]
    [Trait("Category", "Integration")]
    public async Task Get{{EntityName}}_ExistingId_ReturnsOkWithEntity()
    {
        // Arrange
        var id = Guid.NewGuid();
        // Setup test data if needed

        // Act
        var response = await _client.GetAsync($"/api/{{route}}/{id}");

        // Assert
        response.StatusCode.Should().Be(HttpStatusCode.OK);

        var result = await response.Content.ReadFromJsonAsync<{{EntityName}}Dto>();
        result.Should().NotBeNull();
    }

    [Fact]
    [Trait("Story", "{{StoryId}}")]
    [Trait("Category", "Integration")]
    public async Task Get{{EntityName}}_NonExistingId_ReturnsNotFound()
    {
        // Arrange
        var id = Guid.NewGuid();

        // Act
        var response = await _client.GetAsync($"/api/{{route}}/{id}");

        // Assert
        response.StatusCode.Should().Be(HttpStatusCode.NotFound);
    }

    #endregion

    #region POST Endpoints

    [Fact]
    [Trait("Story", "{{StoryId}}")]
    [Trait("Category", "Integration")]
    public async Task Create{{EntityName}}_ValidRequest_ReturnsCreated()
    {
        // Arrange
        var request = new Create{{EntityName}}Request
        {
            // Set properties
        };

        // Act
        var response = await _client.PostAsJsonAsync("/api/{{route}}", request);

        // Assert
        response.StatusCode.Should().Be(HttpStatusCode.Created);
        response.Headers.Location.Should().NotBeNull();

        var result = await response.Content.ReadFromJsonAsync<{{EntityName}}Dto>();
        result.Should().NotBeNull();
    }

    [Fact]
    [Trait("Story", "{{StoryId}}")]
    [Trait("Category", "Integration")]
    public async Task Create{{EntityName}}_InvalidRequest_ReturnsBadRequest()
    {
        // Arrange
        var request = new Create{{EntityName}}Request
        {
            // Invalid properties
        };

        // Act
        var response = await _client.PostAsJsonAsync("/api/{{route}}", request);

        // Assert
        response.StatusCode.Should().Be(HttpStatusCode.BadRequest);
    }

    #endregion

    #region PUT Endpoints

    [Fact]
    [Trait("Story", "{{StoryId}}")]
    [Trait("Category", "Integration")]
    public async Task Update{{EntityName}}_ValidRequest_ReturnsOk()
    {
        // Arrange
        var id = Guid.NewGuid();
        var request = new Update{{EntityName}}Request
        {
            // Set properties
        };

        // Act
        var response = await _client.PutAsJsonAsync($"/api/{{route}}/{id}", request);

        // Assert
        response.StatusCode.Should().Be(HttpStatusCode.OK);
    }

    #endregion

    #region DELETE Endpoints

    [Fact]
    [Trait("Story", "{{StoryId}}")]
    [Trait("Category", "Integration")]
    public async Task Delete{{EntityName}}_ExistingId_ReturnsNoContent()
    {
        // Arrange
        var id = Guid.NewGuid();

        // Act
        var response = await _client.DeleteAsync($"/api/{{route}}/{id}");

        // Assert
        response.StatusCode.Should().Be(HttpStatusCode.NoContent);
    }

    #endregion
}
