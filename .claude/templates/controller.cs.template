// API Controller Template
// Story: {{StoryId}}
// Generated by Claude Code

using MediatR;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using {{Namespace}}.Application.Commands.{{Feature}};
using {{Namespace}}.Application.Queries.{{Feature}};
using {{Namespace}}.Application.DTOs;

namespace {{Namespace}}.Api.Controllers;

/// <summary>
/// API endpoints for {{EntityName}} management.
/// </summary>
[ApiController]
[Route("api/[controller]")]
[Produces("application/json")]
[Authorize]
public class {{EntityName}}sController : ControllerBase
{
    private readonly IMediator _mediator;
    private readonly ILogger<{{EntityName}}sController> _logger;

    public {{EntityName}}sController(
        IMediator mediator,
        ILogger<{{EntityName}}sController> logger)
    {
        _mediator = mediator;
        _logger = logger;
    }

    /// <summary>
    /// Gets all {{EntityName}}s.
    /// </summary>
    /// <returns>List of {{EntityName}}s</returns>
    [HttpGet]
    [ProducesResponseType(typeof(IEnumerable<{{EntityName}}Dto>), StatusCodes.Status200OK)]
    public async Task<IActionResult> GetAll(CancellationToken ct)
    {
        var query = new GetAll{{EntityName}}sQuery();
        var result = await _mediator.Send(query, ct);
        return Ok(result);
    }

    /// <summary>
    /// Gets a {{EntityName}} by ID.
    /// </summary>
    /// <param name="id">The {{EntityName}} ID</param>
    /// <returns>The {{EntityName}} if found</returns>
    [HttpGet("{id:guid}")]
    [ProducesResponseType(typeof({{EntityName}}Dto), StatusCodes.Status200OK)]
    [ProducesResponseType(StatusCodes.Status404NotFound)]
    public async Task<IActionResult> GetById(Guid id, CancellationToken ct)
    {
        var query = new Get{{EntityName}}ByIdQuery(id);
        var result = await _mediator.Send(query, ct);

        if (result is null)
            return NotFound();

        return Ok(result);
    }

    /// <summary>
    /// Creates a new {{EntityName}}.
    /// </summary>
    /// <param name="request">Creation request</param>
    /// <returns>Created {{EntityName}} ID</returns>
    [HttpPost]
    [ProducesResponseType(typeof(Guid), StatusCodes.Status201Created)]
    [ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status400BadRequest)]
    public async Task<IActionResult> Create(
        [FromBody] Create{{EntityName}}Request request,
        CancellationToken ct)
    {
        var command = new Create{{EntityName}}Command(/* map request to command */);
        var result = await _mediator.Send(command, ct);

        if (result.IsFailure)
            return BadRequest(result.Error);

        return CreatedAtAction(
            nameof(GetById),
            new { id = result.Value },
            result.Value);
    }

    /// <summary>
    /// Updates an existing {{EntityName}}.
    /// </summary>
    /// <param name="id">The {{EntityName}} ID</param>
    /// <param name="request">Update request</param>
    [HttpPut("{id:guid}")]
    [ProducesResponseType(StatusCodes.Status200OK)]
    [ProducesResponseType(StatusCodes.Status404NotFound)]
    [ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status400BadRequest)]
    public async Task<IActionResult> Update(
        Guid id,
        [FromBody] Update{{EntityName}}Request request,
        CancellationToken ct)
    {
        var command = new Update{{EntityName}}Command(id, /* map request to command */);
        var result = await _mediator.Send(command, ct);

        if (result.IsFailure)
            return BadRequest(result.Error);

        return Ok();
    }

    /// <summary>
    /// Deletes a {{EntityName}}.
    /// </summary>
    /// <param name="id">The {{EntityName}} ID</param>
    [HttpDelete("{id:guid}")]
    [ProducesResponseType(StatusCodes.Status204NoContent)]
    [ProducesResponseType(StatusCodes.Status404NotFound)]
    [Authorize(Roles = "Admin")]
    public async Task<IActionResult> Delete(Guid id, CancellationToken ct)
    {
        var command = new Delete{{EntityName}}Command(id);
        var result = await _mediator.Send(command, ct);

        if (result.IsFailure)
            return NotFound();

        return NoContent();
    }
}
