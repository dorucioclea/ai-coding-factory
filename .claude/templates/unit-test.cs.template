// Unit Test Template for {{ClassName}}
// Story: {{StoryId}}
// Generated by Claude Code

using Xunit;
using FluentAssertions;
using Moq;
using {{Namespace}};

namespace {{Namespace}}.UnitTests;

public class {{ClassName}}Tests
{
    private readonly Mock<I{{Dependency}}> _{{dependencyLower}}Mock;
    private readonly {{ClassName}} _sut;

    public {{ClassName}}Tests()
    {
        _{{dependencyLower}}Mock = new Mock<I{{Dependency}}>();
        _sut = new {{ClassName}}(_{{dependencyLower}}Mock.Object);
    }

    #region {{MethodName}} Tests

    [Fact]
    [Trait("Story", "{{StoryId}}")]
    [Trait("Category", "Unit")]
    public void {{MethodName}}_ValidInput_ReturnsExpectedResult()
    {
        // Arrange
        var input = /* setup test input */;
        var expectedResult = /* expected result */;

        // Act
        var result = _sut.{{MethodName}}(input);

        // Assert
        result.Should().NotBeNull();
        result.Should().Be(expectedResult);
    }

    [Fact]
    [Trait("Story", "{{StoryId}}")]
    [Trait("Category", "Unit")]
    public void {{MethodName}}_NullInput_ThrowsArgumentNullException()
    {
        // Arrange
        object? input = null;

        // Act
        var act = () => _sut.{{MethodName}}(input!);

        // Assert
        act.Should().Throw<ArgumentNullException>();
    }

    [Fact]
    [Trait("Story", "{{StoryId}}")]
    [Trait("Category", "Unit")]
    public void {{MethodName}}_InvalidInput_ThrowsValidationException()
    {
        // Arrange
        var input = /* invalid input */;

        // Act
        var act = () => _sut.{{MethodName}}(input);

        // Assert
        act.Should().Throw<ValidationException>();
    }

    #endregion
}
